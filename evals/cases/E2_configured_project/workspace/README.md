# Configured Notes

Run `python -m notes_app add "Read code"`.
Run tests with `python -m unittest discover -s project_tests -v`.

Configuration precedence is environment, JSON config file, then built-in defaults.
