# Mini TODO

A small Python CLI project used as a stable Coding Agent evaluation fixture.

## Commands

```text
python -m todo_app --data tasks.json add "Write tests"
python -m todo_app --data tasks.json list
python -m todo_app --data tasks.json done 1
```

## Tests

```text
python -m unittest discover -s project_tests -v
```

